---
type: note
status: "Done"
importance: 3
urgency: 2
area: "[[Systems]]"
cluster: "foundational-infra"
review_date: 2026-07-14
tags: [shellyxz, session, cursor, grok, shell, docs]
---

# 2026-07-14 Sessions — shellyxz (live `~/.config/shell`)

**Back-link**: [[Projects/shellyxz/README|shellyxz]]  
**Live tree**: `~/.config/shell` · public: https://github.com/p10ns11y/shellyxz.sh  
**Branch tip:** **`e74f8f3`** (master)

Dense Cursor night on the shell kernel; Grok also had an untitled session in this cwd.

---

## Session A — Cursor agent-terminal dogfood + docs

**Opening query:** `/onboard` then remove “Run in Ghostty/tmux … not Cursor integrated terminal” — agent viewport terminal + tmux can work; test and delete the gate.

**Shipped (commit wave culminating in `e74f8f3`):**

1. Allow **`ab` / `av`** in Cursor when tmux is set (`8f6d78b`)
2. Finish **PNPM_HOME** removal + resync templates (`200d1cc`); fix `check-template-sync` drift
3. Lead **README** with kernel intent; park detail in `shell.md` (`1c8a74e`)
4. Document **verification cockpit** with live screenshots (`e74f8f3`)

**Energy:** ~2.0

---

## Session B — Grok (untitled, `019f5cba…`)

Host: `~/.config/shell`. Titles empty in index — treat as supporting noise unless a later commit claims product change. No extra portfolio claim beyond Cursor A.

---

## Agent session index (day / late Jul 13 → Jul 14)

| Source | Title / first query |
|--------|---------------------|
| Cursor | /onboard — remove Ghostty/tmux-only gate for agent terminal |
| Cursor | fix template sync drift · README intent-first · verification cockpit screenshots |
| Grok | (untitled) `~/.config/shell` |

---

## Next

- Deepen [[skills/shell-kernel-ontology]] boundary docs if still thin
- Keep co-author hygiene: only add Cursor trailer when Cursor actually authored the work

---

*Titles-only mine; do not copy shell history or secret-loading paths into the vault.*
